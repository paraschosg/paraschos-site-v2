# Contact form → email delivery via Resend (https://resend.com). Free tier is enough.
RESEND_API_KEY=
CONTACT_TO=george@paraschos.site
# Optional: raises GitHub API rate limit from 60/h to 5000/h. Read-only, no scopes needed.
GITHUB_TOKEN=
